# Integration Sync Engine

Real external-API integration: pulls repository/contributor data from GitHub's REST API, normalizes it, stores it idempotently, and pushes it to Airtable — demonstrating authentication, pagination, rate-limit backoff, and audit logging against two genuine third-party systems.

Status: project initialized.